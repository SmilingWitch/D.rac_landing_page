
export default interface DetailsCardProps {
  src: string;   // ruta de la imagen
  header: string
  text: string;  // texto que acompañará la imagen
}

export interface CategoryCardProps {
  src: string;
  name: string;
  text: string;
  number: number;
};